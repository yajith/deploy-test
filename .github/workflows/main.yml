name: test
on: 
  workflow_dispatch:
jobs:
  test-runners:
    runs-on: ubuntu-latest

    steps:
      - name: check docker
        run: |
          sudo apt update
          sudo apt install docker-compose-plugin
          echo $(dpkg -L docker-compose-plugin)
