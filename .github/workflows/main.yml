name: test
on: 
  workflow_dispatch:
jobs:
  test-runners:
    runs-on: ubuntu-latest

    steps:
      - name: check docker
        run: |
          echo $(docker version)
          echo $(docker compose version)
