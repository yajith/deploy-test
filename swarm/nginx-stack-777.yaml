services:
  web:
    image: nginx:latest
    ports:
      - "10777:80"
    secrets:
      - stack_secret_777
    configs:
      - source: stack_config_777
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_777:
    external: true

configs:
  stack_config_777:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
