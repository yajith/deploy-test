services:
  web:
    image: nginx:latest
    ports:
      - "10159:80"
    secrets:
      - stack_secret_159
    configs:
      - source: stack_config_159
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_159:
    external: true

configs:
  stack_config_159:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
