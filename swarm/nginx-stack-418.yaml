services:
  web:
    image: nginx:latest
    ports:
      - "10418:80"
    secrets:
      - stack_secret_418
    configs:
      - source: stack_config_418
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_418:
    external: true

configs:
  stack_config_418:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
