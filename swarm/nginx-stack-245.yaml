services:
  web:
    image: nginx:latest
    ports:
      - "10245:80"
    secrets:
      - stack_secret_245
    configs:
      - source: stack_config_245
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_245:
    external: true

configs:
  stack_config_245:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
