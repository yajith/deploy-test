services:
  web:
    image: nginx:latest
    ports:
      - "10393:80"
    secrets:
      - stack_secret_393
    configs:
      - source: stack_config_393
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_393:
    external: true

configs:
  stack_config_393:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
