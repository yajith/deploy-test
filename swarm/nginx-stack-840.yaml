services:
  web:
    image: nginx:latest
    ports:
      - "10840:80"
    secrets:
      - stack_secret_840
    configs:
      - source: stack_config_840
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_840:
    external: true

configs:
  stack_config_840:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
