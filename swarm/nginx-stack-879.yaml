services:
  web:
    image: nginx:latest
    ports:
      - "10879:80"
    secrets:
      - stack_secret_879
    configs:
      - source: stack_config_879
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_879:
    external: true

configs:
  stack_config_879:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
