services:
  web:
    image: nginx:latest
    ports:
      - "10382:80"
    secrets:
      - stack_secret_382
    configs:
      - source: stack_config_382
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_382:
    external: true

configs:
  stack_config_382:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
