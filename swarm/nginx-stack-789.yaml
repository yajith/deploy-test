services:
  web:
    image: nginx:latest
    ports:
      - "10789:80"
    secrets:
      - stack_secret_789
    configs:
      - source: stack_config_789
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_789:
    external: true

configs:
  stack_config_789:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
