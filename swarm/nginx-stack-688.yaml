services:
  web:
    image: nginx:latest
    ports:
      - "10688:80"
    secrets:
      - stack_secret_688
    configs:
      - source: stack_config_688
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_688:
    external: true

configs:
  stack_config_688:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
