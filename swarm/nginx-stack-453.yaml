services:
  web:
    image: nginx:latest
    ports:
      - "10453:80"
    secrets:
      - stack_secret_453
    configs:
      - source: stack_config_453
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_453:
    external: true

configs:
  stack_config_453:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
