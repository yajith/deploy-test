services:
  web:
    image: nginx:latest
    ports:
      - "10389:80"
    secrets:
      - stack_secret_389
    configs:
      - source: stack_config_389
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_389:
    external: true

configs:
  stack_config_389:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
