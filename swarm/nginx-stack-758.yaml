services:
  web:
    image: nginx:latest
    ports:
      - "10758:80"
    secrets:
      - stack_secret_758
    configs:
      - source: stack_config_758
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_758:
    external: true

configs:
  stack_config_758:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
