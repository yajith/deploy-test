services:
  web:
    image: nginx:latest
    ports:
      - "10776:80"
    secrets:
      - stack_secret_776
    configs:
      - source: stack_config_776
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_776:
    external: true

configs:
  stack_config_776:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
