services:
  web:
    image: nginx:latest
    ports:
      - "10287:80"
    secrets:
      - stack_secret_287
    configs:
      - source: stack_config_287
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_287:
    external: true

configs:
  stack_config_287:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
