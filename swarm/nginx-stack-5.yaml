services:
  web:
    image: nginx:latest
    ports:
      - "10005:80"
    secrets:
      - stack_secret_5
    configs:
      - source: stack_config_5
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_5:
    external: true

configs:
  stack_config_5:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
