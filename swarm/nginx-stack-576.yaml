services:
  web:
    image: nginx:latest
    ports:
      - "10576:80"
    secrets:
      - stack_secret_576
    configs:
      - source: stack_config_576
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_576:
    external: true

configs:
  stack_config_576:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
