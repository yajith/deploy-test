services:
  web:
    image: nginx:latest
    ports:
      - "10667:80"
    secrets:
      - stack_secret_667
    configs:
      - source: stack_config_667
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_667:
    external: true

configs:
  stack_config_667:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
