services:
  web:
    image: nginx:latest
    ports:
      - "10635:80"
    secrets:
      - stack_secret_635
    configs:
      - source: stack_config_635
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_635:
    external: true

configs:
  stack_config_635:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
