services:
  web:
    image: nginx:latest
    ports:
      - "10398:80"
    secrets:
      - stack_secret_398
    configs:
      - source: stack_config_398
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_398:
    external: true

configs:
  stack_config_398:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
