services:
  web:
    image: nginx:latest
    ports:
      - "10739:80"
    secrets:
      - stack_secret_739
    configs:
      - source: stack_config_739
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_739:
    external: true

configs:
  stack_config_739:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
