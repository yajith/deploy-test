services:
  web:
    image: nginx:latest
    ports:
      - "10098:80"
    secrets:
      - stack_secret_98
    configs:
      - source: stack_config_98
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_98:
    external: true

configs:
  stack_config_98:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
