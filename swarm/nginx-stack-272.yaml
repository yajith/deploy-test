services:
  web:
    image: nginx:latest
    ports:
      - "10272:80"
    secrets:
      - stack_secret_272
    configs:
      - source: stack_config_272
        target: /mnt/stack_config.conf
    volumes:
      - stack_volume:/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - webnet

secrets:
  stack_secret_272:
    external: true

configs:
  stack_config_272:
    external: true

volumes:
  stack_volume:

networks:
  webnet:
